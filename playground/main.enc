active class Main
    def main() : unit
        var x = 1
        add(1, x, 3)
        var y = add(x, 2, 1)
        var z = sub(3, add(y, x, 2))
    end
end

fun add (x: int, y: int, z: int) : int
    x + y + z
end

fun sub (x: int, y: int) : int
    x - y
end
